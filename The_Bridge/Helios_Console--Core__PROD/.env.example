# Public (safe for client)
NEXT_PUBLIC_SITE_ENV=development
NEXT_PUBLIC_API_BASE_URL=https://api-dev.example.com
NEXT_PUBLIC_WS_URL=wss://ws-dev.example.com
NEXT_PUBLIC_CHAIN_ID=11155111

# Auth (server/client as needed)
AUTH_ISSUER=https://cognito-idp.us-east-1.amazonaws.com/<poolId>
AUTH_CLIENT_ID=<client-id>
AUTH_AUDIENCE=<api-audience>

# Server-only
API_INTERNAL_TOKEN=<svc-token>
ETH_RPC_URL=https://sepolia.infura.io/v3/<projectId>

# JWT Secret for validation (server-side only)
JWT_SECRET=your-jwt-secret-key